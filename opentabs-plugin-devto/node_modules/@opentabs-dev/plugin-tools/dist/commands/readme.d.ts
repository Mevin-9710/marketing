/**
 * `opentabs-plugin readme` command — generates a user-facing README.md from
 * dist/tools.json and package.json metadata, following the Phase 6 format
 * from the build-plugin skill.
 */
import type { ConfigSchema, ManifestTool } from '@opentabs-dev/shared';
import type { Command } from 'commander';
/** Classify a tool as Read or Write based on its name */
declare const classifyTool: (name: string) => "Read" | "Write";
/**
 * Extract the primary domain from a Chrome match pattern.
 * E.g., `*://*.slack.com/*` → `slack.com`, `*://discord.com/*` → `discord.com`
 */
declare const extractDomain: (pattern: string) => string;
/**
 * Extract the short plugin name from a package name.
 * `@opentabs-dev/opentabs-plugin-slack` → `slack`
 * `opentabs-plugin-discord` → `discord`
 */
declare const extractShortName: (packageName: string) => string;
interface ToolGroup {
    name: string;
    tools: Array<{
        name: string;
        summary: string;
        type: 'Read' | 'Write';
    }>;
}
/** Group tools by their `group` field, preserving first-appearance order */
declare const groupTools: (tools: ManifestTool[]) => ToolGroup[];
interface PluginMeta {
    packageName: string;
    displayName: string;
    description: string;
    domain?: string;
    homepage?: string;
    shortName: string;
}
/** Generate the README markdown string */
declare const generateReadme: (meta: PluginMeta, tools: ManifestTool[], configSchema?: ConfigSchema) => string;
interface ReadmeOptions {
    dryRun?: boolean;
    check?: boolean;
}
declare const handleReadme: (options: ReadmeOptions, projectDir?: string) => Promise<void>;
declare const registerReadmeCommand: (program: Command) => void;
export type { PluginMeta, ReadmeOptions, ToolGroup };
export { classifyTool, extractDomain, extractShortName, generateReadme, groupTools, handleReadme, registerReadmeCommand, };
//# sourceMappingURL=readme.d.ts.map